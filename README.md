# JS Framework CA

This is a course assignment that focuses on the React library with heavy use of Tanstack library. The task was to build a semi working website using modern solutions and knowledge gained throughout the course. The solution was build as a team project.

![Image of the upper part of the website](./images/readMe-img.jpg)

Explore the website by visiting the link - [PROD INC](https://js-frameworkca.netlify.app/)

## Key Features

- Use of TypeScript for validation
- Use Tanstack for Routing and state handling
- Browse product catalog without logging in
- Search products with URL query
- Paginated product listing, with 10 items per page
- View product details with pricing and ratings
- Add products or remove products from a shopping cart
- Cart total calculation

## Tech Stack

Language = TypeScript
Framework = React 19
Build Tool = Vite
Routing = TanStack React Router
State Management = Zustand
Forms = React Hook Form + Zod  
Styling = Bootstrap 5  
API = Noroff Online Shop API  
Hosting = Netlify

## Getting Started

### Prerequisites

- Node.js (v18 or later recommended)
- npm

### Installing

1. Clone the repo:

```bash
git clone https://github.com/NoroffFEU/jsfw-2025-v1-reactjsf26.git
```

2. Install the dependencies:

```bash
npm install
```

3. Run the dependencies:

```bash
npm run dev
```

### Available Scripts

- `npm run dev` - Starts the Vite development server with hot module reloading
- `npm run build` - Compiles TypeScript and bundles the app for production
- `npm run lint` - Displays any ESLint errors in JS and TS files
- `npm run preview` - Serves the production build locally for testing

### Script Details

```json
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
```

### Contact

You can always contact us though official Noroff portal

@Nikita-stud
</br>
@AndrewMoisa
